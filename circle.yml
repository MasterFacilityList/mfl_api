machine:
  environment:
    DATABASE_URL: postgres://ubuntu@localhost:5432/circle_test

dependencies:
  post:
    - pip install fabric

test:
  override:
    - fab test

deployment:
    production:
        branch: master
        commands:
            - fab deploy:
                parallel: true

general:
  artifacts:
    - "htmlcov"
