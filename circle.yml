machine:
  environment:
    DATABASE_URL: postgres://ubuntu@localhost:5432/circle_test

dependencies:
  post:
    - pip install fabric

test:
  pre:
    - cp .env-example .env
  override:
    - fab test

general:
  artifacts:
    - "htmlcov"
